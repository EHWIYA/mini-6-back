version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - ./gradlew build
artifacts:
  base-directory: build/libs
  files:
    - '**/*'
cache:
  paths:
    - .gradle/**/* 